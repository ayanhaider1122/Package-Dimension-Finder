# Package Dimension Finder

This Python script calculates the dimensions and volume of a package based on user input.

## Usage

Run the script and enter the length, width, and height of the item when prompted. The script will output the package dimensions and volume.

## Example

